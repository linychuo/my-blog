{{#*inline "page"}}
	<div class="page-content">
		<div class="wrapper">
			<div class="post">
				<header class="post-header">
					<h1 class="post-title">{{post.title}}</h1>
					<p class="post-meta">{{post.created_date_time}}</p>
				</header>
				<article class="post-content">
					<div class="markup">{{{post.contents}}}</div>
				</article>
			</div>
		</div>
	</div>
{{/inline}}
{{~> (parent)~}}
